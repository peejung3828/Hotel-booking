# Local development overrides — auto-applied by docker compose
services:
  app:
    command: uvicorn backend.main:app --host 0.0.0.0 --port 8000 --reload
    volumes:
      - .:/app                                  # mount source for hot reload
      - uploads:/app/backend/static/uploads     # keep uploads separate

volumes:
  uploads:
